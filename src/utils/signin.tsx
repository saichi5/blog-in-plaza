import type { User } from "@/data";

export async function getSigninUser( username:string, password:string ):Promise<User | undefined> {
  const authUser:User | undefined = undefined;
  
  return authUser;
}